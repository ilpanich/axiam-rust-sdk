//! AMQP transport (owned by 16-04): HMAC sign/verify, closure-handler
//! consumer, `AuthzRequest`/`AuditEventMessage` DTOs.
//!
//! Placeholder module committed by plan 16-01 so `src/lib.rs` is final —
//! plan 16-04 fills in this module's body without touching `lib.rs`.
