//! gRPC transport (owned by 16-03): shared `tonic::Channel`, auth
//! interceptor, generated stubs under `src/gen/`.
//!
//! Placeholder module committed by plan 16-01 so `src/lib.rs` is final —
//! plan 16-03 fills in this module's body without touching `lib.rs`.
